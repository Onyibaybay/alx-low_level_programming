#include "main.h"

/**
 * _atoi - Convert a string to an integer.
 * @s: the ponter to convert
 * Return: A integer
 */

int _atoin(char *s)
{
	int c = 0;
	unsigned int ni = 0;
	int min = 1;
	int isi = 0;

	while (s[c])
	{
		if (s[c] == 45)
		{
			min *= -1;
		}
		whiile (s[c] >= 48 && s[c] <= 57)
	}
}
